## 4.1 `SUMX`

**Що робить:**  
Проходить по кожному рядку таблиці, обчислює вираз і підсумовує результат.

```DAX
Total Revenue (SUMX) :=
SUMX (
    Sales,
    Sales[Quantity] * Sales[Price]
)

4.2 AVERAGEX

Що робить:
Обчислює вираз для кожного рядка та повертає середнє значення.

Average Revenue per Order (AVERAGEX) :=
AVERAGEX (
    SUMMARIZE ( Sales, Sales[OrderID], "OrderRevenue", SUM ( Sales[Revenue] ) ),
    [OrderRevenue]
)


📌 Патерн:

Спочатку агрегуємо до рівня замовлення (SUMMARIZE),

Потім беремо середнє по цих замовленнях, а не по окремих рядках.

4.3 MINX / 4.4 MAXX

Що роблять:
Проходять по таблиці, обчислюють вираз і повертають мінімальне / максимальне значення.

Min Order Revenue (MINX) :=
MINX (
    SUMMARIZE ( Sales, Sales[OrderID], "OrderRevenue", SUM ( Sales[Revenue] ) ),
    [OrderRevenue]
)

Max Order Revenue (MAXX) :=
MAXX (
    SUMMARIZE ( Sales, Sales[OrderID], "OrderRevenue", SUM ( Sales[Revenue] ) ),
    [OrderRevenue]
)


📌 Часто використовуються для знаходження:

найдешевшого / найдорожчого замовлення,

найнижчого / найвищого LTV, чека, тощо.

4.5 COUNTX

Що робить:
Рахує, скільки рядків у таблиці дають не-порожній результат для виразу.

Customers with Revenue (COUNTX) :=
COUNTX (
    SUMMARIZE ( Sales, Sales[CustomerID], "CustomerRevenue", SUM ( Sales[Revenue] ) ),
    [CustomerRevenue]
)


📌 Відмінність від DISTINCTCOUNT:

DISTINCTCOUNT ( Sales[CustomerID] ) рахує унікальні ID незалежно від виручки.

COUNTX можна комбінувати з умовами (наприклад, рахувати тільки клієнтів з Revenue > 0).

4.6 RANKX

Що робить:
Присвоює ранг кожному елементу у контексті (категорії, клієнти, продукти) за заданим виразом.

Rank Product by Revenue (RANKX) :=
RANKX (
    ALL ( Sales[Product] ),
    CALCULATE ( SUM ( Sales[Revenue] ) ),
    ,
    DESC,
    Dense        -- або Skip / Default
)


📌 Пояснення:

ALL ( Sales[Product] ) — знімаємо фільтр по продуктам, щоб ранжувати у глобальному контексті.

Вираз ранжування: сумарна виручка по продукту.

DESC — від більшого до меншого.

Dense — ранги без пропусків (1,2,2,3…).


4.7 Патерн: SUMX + FILTER (умовна сума)

Що робить:
Дозволяє рахувати суму по складному виразу з додатковими умовами.

Revenue for High-Price Items (SUMX + FILTER) :=
SUMX (
    FILTER (
        Sales,
        Sales[Price] > 100
    ),
    Sales[Quantity] * Sales[Price]
)


📌 Чому не просто CALCULATE ( SUM ( Sales[Revenue] ), Sales[Price] > 100 )?
Бо у виразі можна використати будь-яку формулу (не лише одну колонку).


4.8 Патерн: ADDCOLUMNS + X-функції

Що робить:
Додає обчислювані колонки “на льоту” і дозволяє робити складні обчислення по групах.

Top Customers by Avg Order (ADDCOLUMNS + AVERAGEX) :=
VAR OrdersPerCustomer =
    ADDCOLUMNS (
        SUMMARIZE ( Sales, Sales[CustomerID] ),
        "AvgOrder",
            AVERAGEX (
                FILTER ( Sales, Sales[CustomerID] = EARLIER ( Sales[CustomerID] ) ),
                Sales[Revenue]
            )
    )
RETURN
TOPN ( 10, OrdersPerCustomer, [AvgOrder], DESC )


📌 Ідея:

SUMMARIZE → одна строка на клієнта,

ADDCOLUMNS → додаємо йому середній чек,

TOPN → забираємо топ-10.

Цей патерн часто використовується для створення віртуальних таблиць у мірах.
